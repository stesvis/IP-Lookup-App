type CityInfo = {
  accuracyRadius?: string;
  cityName?: string;
  countryCode?: string;
  error?: string;
  ipAddress?: string;
  postalCode?: string;
  timeZone?: string;
};

export default CityInfo;
